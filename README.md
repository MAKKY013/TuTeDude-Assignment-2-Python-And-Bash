# TuTeDude Assignment 2 – Python and Bash : Write to a File

## 📌 Task
Write a program to create a text file and write some content to it, using file functions like `open` and `write` in Python, and redirection operators in Bash.

---

## 🐍 Python Solution (`write_file.py`)
```bash
python3 write_file.py


Example Output:
Content written to output.txt successfully.


🖥️ Bash Solution (write_file.sh)

bash write_file.sh

Example Output:
Content written to output.txt successfully.


📂 Repository Structure:
TuTeDude_Assignment-3_Python-and-Bash-Write-to-File/
│
├── write_file.py   # Python solution
├── write_file.sh   # Bash solution
└── README.md       # Documentation
